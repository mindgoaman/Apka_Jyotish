const assetsPath = "../assets/";

const bg1 = require(`${assetsPath}bg1.png`);
const onboarding1 = require(`${assetsPath}onboarding1.png`);
const onboarding2 = require(`${assetsPath}onboarding2.png`);
const onboarding3 = require(`${assetsPath}onboarding3.png`);
const onboarding4 = require(`${assetsPath}onboarding4.png`);
const next = require(`${assetsPath}next.png`);
const background = require(`${assetsPath}background.png`);
const logo1 = require(`${assetsPath}logo1.png`);
const email = require(`${assetsPath}email.png`);
const password = require(`${assetsPath}password.png`);
const view_password = require(`${assetsPath}hide_password.png`);
const hide_password = require(`${assetsPath}view_password.png`);
const button = require(`${assetsPath}button.png`);
const address = require(`${assetsPath}address.png`);
const business_name = require(`${assetsPath}business_name.png`);
const check_selected = require(`${assetsPath}check_selected.png`);
const check_unselected = require(`${assetsPath}check_unselected.png`);
const radio_selected = require(`${assetsPath}radio_selected.png`);
const radio_unselected = require(`${assetsPath}radio_unselected.png`);
const full_name = require(`${assetsPath}full_name.png`);
const info = require(`${assetsPath}info.png`);
const licence = require(`${assetsPath}licence.png`);
const profile_pic = require(`${assetsPath}profile_pic.png`);
const web = require(`${assetsPath}web.png`);
const phone = require(`${assetsPath}phone.png`);

const home_selected = require(`${assetsPath}home_selected.png`);
const home_unselected = require(`${assetsPath}home_unselected.png`);
const shortlist_selected = require(`${assetsPath}shortlist_selected.png`);
const shortlist_unselected = require(`${assetsPath}shortlist_unselected.png`);
const fav_selected = require(`${assetsPath}fav_selected.png`);
const fav_unselected = require(`${assetsPath}fav_unselected.png`);
const subscribed_selected = require(`${assetsPath}subscribed_selected.png`);
const subscribed_unselected = require(`${assetsPath}subscribed_unselected.png`);

const history = require(`${assetsPath}history.png`);
const notifications = require(`${assetsPath}notifications.png`);
const tnc = require(`${assetsPath}tnc.png`);
const contact_us = require(`${assetsPath}contact_us.png`);
const logout = require(`${assetsPath}logout.png`);
const account = require(`${assetsPath}account.png`);
const navigation = require(`${assetsPath}navigation.png`);
const menu = require(`${assetsPath}menu.png`);
const location = require(`${assetsPath}Location.png`);
const searchBarBackground = require(`${assetsPath}Home_Bg.jpg`);
const searchIcon = require(`${assetsPath}Search.png`);
const backButton = require(`${assetsPath}backArrow.png`);
const alarm = require(`${assetsPath}alarm.png`);
const calender = require(`${assetsPath}calendar.png`);
const nextImage2 = require(`${assetsPath}Next2.png`);
const caravanDetailBackground = require(`${assetsPath}Combined_Shape.png`);
const dot = require(`${assetsPath}Dot.png`);
const SquareFeet = require(`${assetsPath}SquareFeet.png`);
const Description = require(`${assetsPath}Description.png`);
const NoOfBathrooms = require(`${assetsPath}NumberOfBathrooms.png`);
const NoOfRooms = require(`${assetsPath}NumberOfRooms.png`);
const addProperty = require(`${assetsPath}AddProperty.png`);
const cross = require(`${assetsPath}Cross.png`);
const not_approved = require(`${assetsPath}Not_Approved.png`);
const share = require(`${assetsPath}share.png`);
const business_icon = require(`${assetsPath}Business.png`);
const delete_icon = require(`${assetsPath}Delete.png`);
const business_website_icon = require(`${assetsPath}Business_Website.png`);
const pdf_image = require(`${assetsPath}pdf.png`);
const gold_plan = require(`${assetsPath}Gold_Plan.png`);
const silver_plan = require(`${assetsPath}Silver_Plan.png`);
const platinum_plan = require(`${assetsPath}Platinum_Plan.png`);

export {
  bg1,
  onboarding1,
  onboarding2,
  onboarding3,
  onboarding4,
  next,
  background,
  logo1,
  email,
  password,
  view_password,
  hide_password,
  button,
  address,
  business_name,
  check_selected,
  check_unselected,
  radio_selected,
  radio_unselected,
  full_name,
  info,
  licence,
  profile_pic,
  web,
  phone,
  home_selected,
  home_unselected,
  shortlist_selected,
  shortlist_unselected,
  fav_selected,
  fav_unselected,
  subscribed_selected,
  subscribed_unselected,
  history,
  notifications,
  tnc,
  contact_us,
  logout,
  account,
  navigation,
  menu,
  location,
  searchBarBackground,
  searchIcon,
  backButton,
  alarm,
  calender,
  nextImage2,
  caravanDetailBackground,
  dot,
  addProperty,
  Description,
  SquareFeet,
  NoOfBathrooms,
  NoOfRooms,
  cross,
  not_approved,
  share,
  business_icon,
  delete_icon,
  business_website_icon,
  pdf_image,
  gold_plan,
  silver_plan,
  platinum_plan
};
